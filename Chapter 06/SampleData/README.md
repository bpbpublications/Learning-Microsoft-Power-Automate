# Sample Data

This folder is reserved for synthetic data used by Chapter 6 exercises.

## Recommended Assets

Sample assets may include:

- API request and response examples
- SQL test data
- CSV files
- JSON payloads
- Python input/output examples

Use synthetic or non-sensitive data only.

When assets are added, document which exercise uses each file and keep the filenames referenced by the corresponding Markdown article.
